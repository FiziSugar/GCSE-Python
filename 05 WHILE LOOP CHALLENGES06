#Ask the user to enter a number between 10 and 20
#If they enter a value under 10, display the message “too low” then ask them to try again
#If they enter a value above 20, display the message “too high” and ask them to try again.
#Ask them to “try again”
#Keep repeating this until (while) they enter a value that is between 10 and 20 and
#then display the message “thank you”


num = int(input("Enter a number between 10 and 20: "))

while num < 10:
    print("Too low")
    num = int(input("Try again: "))
while num > 20:
    print("Too high")
    num = int(input("Try again: "))
print("Thank you!")
